#include "GameEngine.hpp"

int main()
{
    // Declare a gameplay instance
    GameEngine gameEngine;
    
    // Start the gameplay engine
    gameEngine.run();
    
    // Exit when the engine is stopped
    return EXIT_SUCCESS;
}
